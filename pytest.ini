[pytest]
testpaths = tests
pythonpath = app .
asyncio_mode = auto
markers =
    integration: live Docker/Testcontainers pgvector integration tests (deselect with -m "not integration")
env =
    DATABASE_URL=sqlite:///:memory:
    PROJECT_NAME=test
    REDIS_URL=redis://localhost:6379/0
