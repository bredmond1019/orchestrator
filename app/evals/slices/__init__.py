"""Domain-specific eval slice builders (Bastion Program Block OR.U).

Docstring-only module marker, per ``planning/or-u-eval-engine/tasks.md``
Notes — later tasks/submodules (starting with ``evals.slices.coding``, Task
5) import their own module directly (e.g. ``from evals.slices.coding import
build_coding_slice``) and never edit this file, which keeps task file
ownership disjoint for parallel-merge safety.
"""
